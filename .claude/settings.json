{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && bun tsc --noEmit --project mcp/tsconfig.json 2>&1 && bun test 2>&1 || echo 'TEST_GUARDIAN: Failures detected — check output above'",
            "async": true,
            "timeout": 60
          }
        ]
      }
    ]
  }
}
